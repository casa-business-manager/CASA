import React from "react";
import { useParams } from "react-router-dom";

const OrganizationHome = ({}) => {
	const { orgId } = useParams();

	return <>TODO: Make a dashboard/home page</>;
};

export default OrganizationHome;
